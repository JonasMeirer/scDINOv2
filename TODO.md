- [x] normalisation for inference need to be implemented correctly (currently there is no normalisation based on raw images, just for the preprocesed ones) (DONE)
- [x] figure out when cellpose if failing, based on new feature.csv that should have info on which cell crops have a failed cellpose
- [ ] implement dino training


