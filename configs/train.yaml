defaults:
  - datamodule: chronotype
  - model: dinov2
  - trainer: default
  - logging: console
  - _self_

mode: "train"
seed: 42
max_train_batches: null # null trains on the full epoch; set an integer to cap batches per epoch when debugging
eval_only_at_end: false

hardware:
  accelerator: "gpu"
  devices: 1

# Hydra configuration
hydra:
  run:
    dir: outputs/train/${model.name}_${datamodule.dataset}/${now:%Y-%m-%d}/${now:%H-%M-%S}
  job:
    chdir: true