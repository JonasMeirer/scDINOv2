# @package _global_
# Ablation: disable random rotation.

defaults:
  - override /model: dinov2
  - override /logging: console

max_batches: null
datamodule:
  transforms:
    rr_prob: 0.0

hydra:
  sweep:
    dir: outputs/benchmark/augmentation
    subdir: no_rotation_seed${seed}
