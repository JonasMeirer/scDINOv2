# @package _global_
# Ablation: Change to center crop

defaults:
  - override /model: dinov2
  - override /logging: console

max_batches: null
datamodule:
  transforms:
    do_center_crop: true

hydra:
  sweep:
    dir: outputs/benchmark/augmentation
    subdir: do_centercrop_seed${seed}
