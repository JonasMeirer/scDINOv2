# @package _global_
# Ablation: all augmentations disabled (only random crops + normalize).

defaults:
  - override /model: dinov2
  - override /logging: console

max_batches: null
datamodule:
  transforms:
    do_center_crop: true

hydra:
  sweep:
    dir: outputs/benchmark/augmentation
    subdir: do_centercrop_seed${seed}
