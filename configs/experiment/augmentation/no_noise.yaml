# @package _global_
# Ablation: disable Gaussian noise.

defaults:
  - override /model: dinov2
  - override /logging: console

max_batches: null
datamodule:
  transforms:
    gaussian_noise_prob: 0.0

hydra:
  sweep:
    dir: outputs/benchmark/augmentation
    subdir: no_noise_seed${seed}
