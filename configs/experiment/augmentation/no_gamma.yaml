# @package _global_
# Ablation: disable per-channel gamma correction.

defaults:
  - override /model: dinov2
  - override /logging: console

max_batches: null
datamodule:
  transforms:
    gamma_prob: 0.0

hydra:
  sweep:
    dir: outputs/benchmark/augmentation
    subdir: no_gamma_seed${seed}
