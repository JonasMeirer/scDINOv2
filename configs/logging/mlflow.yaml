name: "mlflow"
tracking_uri: ${oc.env:MLFLOW_TRACKING_URI,"file:./mlruns"}
run_name: ${model.name}_${datamodule.dataset}
tags:
  mode: ${mode}
  model: ${model.name}
  dataset: ${datamodule.dataset}
log_model: false
